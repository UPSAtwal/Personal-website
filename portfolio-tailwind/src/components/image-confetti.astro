---

---

<imageConfetti class="border-white border-rounded rounded-full w-1/2 mx-auto data-tilt object-cover">
<slot />
</imageConfetti>

<script>
	import confetti from 'canvas-confetti';

const image = document.body.querySelector('img');

	image.addEventListener('mouseenter', () => {
	confetti({
    particleCount: 100,
    spread: 360,
    origin: { y: 0.7 }
	});
});